# Multi-speaker ASR project — pinned to the versions that actually work on
# Windows + RTX 4060 (8GB) + CUDA 12.4 in the `TTS` conda env.
#
# DO NOT pip-install this blindly into a fresh env on Windows — torch must be
# installed FIRST from the official CUDA index, otherwise pip will pull the
# CPU build. Recommended bootstrap:
#
#     conda create -n TTS python=3.10 -y
#     conda activate TTS
#     pip install torch==2.5.1 torchaudio==2.5.1 torchvision==0.20.1 \
#         --index-url https://download.pytorch.org/whl/cu124
#     pip install -r requirements.txt
#     pip install whisperx==3.4.2 --no-deps          # avoid pulling torch 2.8
#
# On Linux/cloud, the faster-whisper / ctranslate2 path "just works"; on
# Windows we use openai-whisper as the local ASR backend (see configs/models.yaml).

# ---- Core (pinned hard) ----
numpy<2.3                          # 2.2.6 verified
torch==2.5.1                       # +cu124 from pytorch index, see header
torchaudio==2.5.1
torchvision==0.20.1
transformers>=4.51,<5
huggingface-hub>=0.25.0
setuptools<80                      # 82+ removed pkg_resources, faster-whisper still needs it

# ---- ASR backends ----
openai-whisper>=20240930           # local backend (no ctranslate2)
faster-whisper==1.1.1              # cloud backend; keep installed but unused locally
ctranslate2==4.5.0                 # paired with cuDNN 9 wheels below
nvidia-cudnn-cu12>=9.22            # Windows: ctranslate2 needs these on DLL path
nvidia-cublas-cu12>=12.9
# whisperx is installed separately with --no-deps to avoid forcing torch 2.8

# ---- Speaker diarization ----
pyannote.audio==3.4.0
pyannote.core>=5.0
pyannote.pipeline>=3.0
pyannote.metrics>=3.2,<4

# ---- Audio frontend ----
demucs==4.0.1                      # use demucs.separate API (4.1.x not on PyPI)
librosa>=0.10.2
soundfile>=0.12.1
pydub>=0.25.1
ffmpeg-python>=0.2.0
onnxruntime>=1.19                  # required by torchmetrics→pyannote chain

# ---- Retrieval ----
FlagEmbedding>=1.4.0               # BGE-M3 dense + sparse
rank-bm25>=0.2.2
faiss-cpu>=1.8.0

# ---- LLM clients ----
anthropic>=0.40
openai>=1.50

# ---- GUI ----
gradio>=5.0

# ---- Utilities ----
python-dotenv>=1.0
pyyaml>=6.0
tqdm>=4.66
rich>=13.7
pandas>=2.2.3
omegaconf>=2.3

# ---- Evaluation ----
jiwer>=3.0
textgrid>=1.5
